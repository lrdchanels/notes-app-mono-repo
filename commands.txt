# React
  # Create new react app
  npx create-react-app "name"

# Node
  # Create new node project
  npm init

  # Live server
  npm install nodemon -D

  # express
  npm install express

  # eslint with standard
  npm install standard -D
  "eslintConfig": {
    "extends": "./node_modules/standard/eslintrc.json"
  }

  # cors to share resources between ports
  npm install cors

  # mongoose
  npm install mongoose

  # use env
  npm install dotenv

  # testing 
  npm install jest -D

  # testing endpoints
  npm install supertest -D

  # encrypt passwords
  npm install bcrypt